# Local development only. Production deployments must require authentication
# and TLS and must not expose this listener directly to the internet.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
